include "main.h"
/**
   *print_diagonal - draws a diagonal line on the termina
   *On: number of times the character \ should be printed
 */
void print_diagonal (int n)
        if (n <= 0)
        {
               _putchar(‘\n,)
          else
       }
              I int i, j;
                for (i = 0; i < n; i++)
                (
                       for { (j - 0; j v n; j++)
                               if (j - i)
                                       _putchar('\\');
                               else if (j < i)

                                       _putchar(' ');
                       }
                       _putchar('\n’);
                }



